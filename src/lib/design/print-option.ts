export const PRINT_OPTION_TYPES=["ONE_SIDE","TWO_SIDES","FULL_WRAP"] as const;export type PrintOptionType=typeof PRINT_OPTION_TYPES[number];export const PRINT_PLACEMENTS=["FRONT","LEFT","RIGHT"] as const;export type PrintPlacement=typeof PRINT_PLACEMENTS[number];
export type PrintOption={type:PrintOptionType;label:string;priceSatang:number;isEnabled:boolean;widthCm:number;heightCm:number};
export const DEFAULT_PRINT_OPTIONS:PrintOption[]=[{type:"ONE_SIDE",label:"1 ด้าน",priceSatang:15000,isEnabled:true,widthCm:8,heightCm:8},{type:"TWO_SIDES",label:"2 ด้าน",priceSatang:18000,isEnabled:true,widthCm:20,heightCm:9},{type:"FULL_WRAP",label:"รอบแก้ว",priceSatang:19900,isEnabled:true,widthCm:20,heightCm:9}];
export function parsePrintOptions(value:unknown):PrintOption[]{if(!Array.isArray(value))throw new Error("ตัวเลือกงานพิมพ์ไม่ถูกต้อง");const seen=new Set<string>();const options=value.map(raw=>{if(!raw||typeof raw!=="object")throw new Error("ตัวเลือกงานพิมพ์ไม่ถูกต้อง");const item=raw as Record<string,unknown>,type=String(item.type) as PrintOptionType;if(!PRINT_OPTION_TYPES.includes(type)||seen.has(type))throw new Error("ประเภทงานพิมพ์ไม่ถูกต้อง");seen.add(type);const defaults=DEFAULT_PRINT_OPTIONS.find(option=>option.type===type)!;const priceSatang=Math.round(Number(item.priceSatang)),widthCm=Number(item.widthCm),heightCm=Number(item.heightCm);if(!Number.isInteger(priceSatang)||priceSatang<0||priceSatang>10_000_000||!Number.isFinite(widthCm)||widthCm<=0||widthCm>30||!Number.isFinite(heightCm)||heightCm<=0||heightCm>20)throw new Error("ราคา/ขนาดงานพิมพ์ไม่ถูกต้อง");return{type,label:defaults.label,priceSatang,isEnabled:Boolean(item.isEnabled),widthCm,heightCm}});if(options.length!==3||!options.some(option=>option.isEnabled))throw new Error("ต้องมีตัวเลือกงานพิมพ์ครบและเปิดอย่างน้อย 1 แบบ");return PRINT_OPTION_TYPES.map(type=>options.find(option=>option.type===type)!)}
export function parsePrintSelection(options:PrintOption[],typeValue:unknown,placementValue:unknown){const type=String(typeValue) as PrintOptionType,option=options.find(item=>item.type===type&&item.isEnabled);if(!option)throw new Error("รูปแบบงานพิมพ์ไม่พร้อมใช้งาน");const placement=type==="ONE_SIDE"?String(placementValue) as PrintPlacement:null;if(type==="ONE_SIDE"&&!PRINT_PLACEMENTS.includes(placement as PrintPlacement))throw new Error("ตำแหน่งพิมพ์ไม่ถูกต้อง");return{option,placement}}
