import assert from "node:assert/strict";import test from "node:test";import {DEFAULT_PRINT_OPTIONS,parsePrintOptions,parsePrintSelection} from "./print-option";
test("default print prices",()=>assert.deepEqual(DEFAULT_PRINT_OPTIONS.map(x=>x.priceSatang),[15000,17000,19900]));test("validates one-side placement",()=>{assert.equal(parsePrintSelection(DEFAULT_PRINT_OPTIONS,"ONE_SIDE","LEFT").placement,"LEFT");assert.throws(()=>parsePrintSelection(DEFAULT_PRINT_OPTIONS,"ONE_SIDE","BACK"))});test("requires an enabled option",()=>assert.throws(()=>parsePrintOptions(DEFAULT_PRINT_OPTIONS.map(x=>({...x,isEnabled:false})))));
