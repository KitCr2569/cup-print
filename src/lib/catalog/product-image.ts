export const MAX_PRODUCT_IMAGE_SIZE=5_000_000;
const EXTENSIONS:Record<string,string>={"image/jpeg":"jpg","image/png":"png","image/webp":"webp"};
export async function validateProductImage(value:unknown){if(!(value instanceof File)||value.size===0||value.size>MAX_PRODUCT_IMAGE_SIZE)throw new Error("กรุณาใช้รูป JPG, PNG หรือ WebP ไม่เกิน 5 MB");const extension=EXTENSIONS[value.type];if(!extension)throw new Error("รองรับเฉพาะรูป JPG, PNG และ WebP");const bytes=new Uint8Array(await value.slice(0,12).arrayBuffer());if(!hasValidSignature(value.type,bytes))throw new Error("เนื้อหาไฟล์ไม่ตรงกับชนิดรูป");return{file:value,extension}}
export function hasValidSignature(type:string,bytes:Uint8Array){if(type==="image/jpeg")return bytes[0]===0xff&&bytes[1]===0xd8&&bytes[2]===0xff;if(type==="image/png")return bytes.slice(0,8).every((value,index)=>value===[0x89,0x50,0x4e,0x47,0x0d,0x0a,0x1a,0x0a][index]);if(type==="image/webp")return new TextDecoder().decode(bytes.slice(0,4))==="RIFF"&&new TextDecoder().decode(bytes.slice(8,12))==="WEBP";return false}
