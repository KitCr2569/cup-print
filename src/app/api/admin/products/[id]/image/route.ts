import { isAdmin } from "@/lib/auth/admin";import { validateProductImage } from "@/lib/catalog/product-image";import { query } from "@/lib/database/db";import { getFileStorage } from "@/lib/storage";
export const runtime="nodejs";
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){if(!await isAdmin())return Response.json({error:"Unauthorized"},{status:401});try{const {id}=await params;if(!/^[a-z0-9]+(?:-[a-z0-9]+)*$/.test(id))return Response.json({error:"รหัสสินค้าไม่ถูกต้อง"},{status:400});const exists=(await query("SELECT 1 FROM products WHERE id=$1",[id])).rowCount;if(!exists)return Response.json({error:"ไม่พบสินค้า"},{status:404});const {file,extension}=await validateProductImage((await request.formData()).get("image"));const imagePath=await getFileStorage().storeFile(`products/${id}`,file,extension);await query("UPDATE products SET image_path=$1,updated_at=now() WHERE id=$2",[imagePath,id]);return Response.json({ok:true,imageUrl:`/api/products/${id}/image`})}catch(error){console.error("Unable to upload product image",error);return Response.json({error:error instanceof Error?error.message:"อัปโหลดรูปไม่สำเร็จ"},{status:400})}}
