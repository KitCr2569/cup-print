import {isAdmin} from "@/lib/auth/admin";import {query} from "@/lib/database/db";import {getFileStorage} from "@/lib/storage";import {validateHeroVideo} from "@/lib/site-media/video";
export const runtime="nodejs";
export async function POST(request:Request){if(!await isAdmin())return Response.json({error:"Unauthorized"},{status:401});try{const {file,extension}=await validateHeroVideo((await request.formData()).get("video"));const storagePath=await getFileStorage().storeFile("site/home-hero",file,extension);await query("INSERT INTO site_media(media_key,storage_path,content_type,updated_at) VALUES ('home_hero_video',$1,$2,now()) ON CONFLICT(media_key) DO UPDATE SET storage_path=EXCLUDED.storage_path,content_type=EXCLUDED.content_type,updated_at=now()",[storagePath,file.type]);return Response.json({ok:true})}catch(error){console.error("Unable to upload hero video",error);return Response.json({error:error instanceof Error?error.message:"อัปโหลดวิดีโอไม่สำเร็จ"},{status:400})}}
