import {isAdmin} from "@/lib/auth/admin";import {query} from "@/lib/database/db";import {loadFile} from "@/lib/storage/local";
export const runtime="nodejs";export async function GET(_:Request,ctx:RouteContext<"/api/admin/designs/[id]/print">){if(!await isAdmin())return new Response("Unauthorized",{status:401});const {id}=await ctx.params;const row=(await query<{print_path:string}>("SELECT print_path FROM designs WHERE id=$1",[id])).rows[0];if(!row)return new Response("Not found",{status:404});return new Response(await loadFile(row.print_path),{headers:{"content-type":"image/png","content-disposition":`attachment; filename="cupstory-${id}.png"`}})}
