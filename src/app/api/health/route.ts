import {query} from "@/lib/database/db";export const dynamic="force-dynamic";export async function GET(){try{await query("SELECT 1");const r2Ready=["R2_ACCOUNT_ID","R2_ACCESS_KEY_ID","R2_SECRET_ACCESS_KEY","R2_BUCKET_NAME"].every(name=>Boolean(process.env[name]?.trim()));return Response.json({status:"ok",database:"ok",storage:r2Ready?"configured":"not_configured"},{headers:{"cache-control":"no-store"}})}catch(error){console.error("Health check failed",error);return Response.json({status:"degraded",database:"unavailable"},{status:503,headers:{"cache-control":"no-store"}})}}
