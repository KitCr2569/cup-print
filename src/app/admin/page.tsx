import {redirect} from "next/navigation";import Link from "next/link";import Image from "next/image";import {isAdmin} from "@/lib/auth/admin";import {query} from "@/lib/database/db";import OrderActions from "@/components/admin/order-actions";
type Row={id:string;order_number:string;status:string;customer_name:string;phone:string;total_satang:number;created_at:string;design_id:string;quantity:number;preview_path:string};
export const dynamic="force-dynamic";export default async function AdminPage(){if(!await isAdmin())redirect("/admin/login");const rows=(await query<Row>("SELECT o.id,o.order_number,o.status,o.customer_name,o.phone,o.total_satang,o.created_at,oi.design_id,oi.quantity,d.preview_path FROM orders o JOIN order_items oi ON oi.order_id=o.id JOIN designs d ON d.id=oi.design_id ORDER BY o.created_at DESC LIMIT 100")).rows;return <main className="admin-page"><aside className="admin-side"><Link href="/" className="brand"><span className="brand-dot">C</span><span>Cup Story</span></Link><p>ADMIN PANEL</p><div className="admin-links"><Link className="active" href="/admin">คำสั่งซื้อ <span>{rows.length}</span></Link><Link href="/admin/products">สินค้า</Link><Link href="/admin/site">หน้าเว็บไซต์</Link></div><Link href="/">← กลับหน้าร้าน</Link></aside><section className="admin-content"><header><p className="eyebrow">PRODUCTION DESK</p><h1>คำสั่งซื้อ</h1><p>ตรวจแบบ ดาวน์โหลดไฟล์พิมพ์ และอัปเดตสถานะการผลิต</p></header><div className="admin-orders">{rows.length===0?<div className="empty-order">ยังไม่มีคำสั่งซื้อ</div>:rows.map(o=><article key={o.id}><Image src={`/api/designs/${o.design_id}/preview`} alt="แบบลูกค้า" width={100} height={58} unoptimized/><div className="order-main"><small>{new Date(o.created_at).toLocaleString("th-TH")}</small><h2>{o.order_number}</h2><p>{o.customer_name} • {o.phone}</p></div><div><small>จำนวน</small><b>{o.quantity} ใบ</b></div><div><small>ยอดรวม</small><b>฿{(o.total_satang/100).toLocaleString("th-TH")}</b></div><OrderActions id={o.id} status={o.status}/><a className="download-button" href={`/api/admin/designs/${o.design_id}/print`}>ดาวน์โหลดไฟล์พิมพ์</a></article>)}</div></section></main>}
