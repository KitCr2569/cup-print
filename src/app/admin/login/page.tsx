"use client";import {FormEvent,useState} from "react";import {useRouter} from "next/navigation";import Link from "next/link";
export default function AdminLogin(){const router=useRouter();const [error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const password=String(new FormData(e.currentTarget).get("password"));const r=await fetch("/api/admin/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({password})});if(r.ok){router.push("/admin");router.refresh()}else setError("รหัสผ่านไม่ถูกต้อง")}return <main className="admin-login"><form onSubmit={submit}><Link href="/" className="brand"><span className="brand-dot">C</span><span>Cup Story</span></Link><p>ADMIN PANEL</p><h1>เข้าสู่ระบบร้าน</h1><label>รหัสผ่าน<input name="password" type="password" autoFocus required/></label>{error&&<small>{error}</small>}<button className="primary wide">เข้าสู่ระบบ</button></form></main>}
